<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kami.cloud.lease.web.app.mapper.LabelInfoMapper">

    <select id="selectListByRoomIds" resultType="com.kami.cloud.lease.model.entity.LabelInfo">
        SELECT
        li.id,
        li.type,
        li.name,
        rl.room_id AS roomId   <!-- 关键：取别名，映射到 LabelInfo 的 roomId 字段 -->
        FROM label_info li
        INNER JOIN room_label rl ON li.id = rl.label_id
        WHERE rl.room_id IN
        <foreach collection="roomIds" item="roomId" open="(" separator="," close=")">
            #{roomId}
        </foreach>
        AND rl.is_deleted = 0
        AND li.is_deleted = 0
    </select>
    <select id="selectListByApartmentId" resultType="com.kami.cloud.lease.model.entity.LabelInfo">
        select id,
               type,
               name
        from label_info
        where is_deleted = 0
          and id in (select label_id
                     from apartment_label
                     where is_deleted = 0
                       and apartment_id = #{id})
    </select>
    <select id="selectListByRoomId" resultType="com.kami.cloud.lease.model.entity.LabelInfo">
        select id,
               type,
               name
        from label_info
        where is_deleted = 0
          and id in (select label_id
                     from room_label
                     where is_deleted = 0
                       and room_id = #{id})
    </select>
</mapper>
